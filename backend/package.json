{
  "name": "backend",
  "version": "1.0.0",
  "description": "AI Job Matching Platform Backend",
  "type": "module",

  "scripts": {
    "validate": "sam validate",
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local:fetchJobs": "sam local invoke FetchJobsFunction -e events/searchJobs.json"
  },

  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}